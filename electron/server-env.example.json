{
  "_readme": "Save a copy as server-env.json in the same folder as Help → Open workspace sync folder. Use FIREBASE_* keys to sync this desktop app to your cloud workspace (same values as hosted deployment). Private key must use \\n newlines inside the JSON string. To enable AI without saving a session cookie, set GOOGLE_API_KEY (or GEMINI_API_KEY). Restart the desktop app after saving.",
  "FIREBASE_PROJECT_ID": "",
  "FIREBASE_CLIENT_EMAIL": "",
  "FIREBASE_PRIVATE_KEY": "",
  "GOOGLE_API_KEY": "",
  "GEMINI_API_KEY": ""
}
